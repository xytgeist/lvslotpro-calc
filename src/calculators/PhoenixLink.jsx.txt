import { useState, useEffect } from 'react'
import { Line } from 'react-chartjs-2'
import {
  Chart as ChartJS,
  CategoryScale,
  LinearScale,
  PointElement,
  LineElement,
  Title,
  Tooltip,
  Legend,
} from 'chart.js'

ChartJS.register(CategoryScale, LinearScale, PointElement, LineElement, Title, Tooltip, Legend)

const MUST_HIT = 1888

// Then paste ALL your working code here, starting from "function App() {" but change the function name to:

function PhoenixLink({ onBack }) {
  // ... your entire working code goes here ...

  // At the very bottom, before the final closing, add a back button if you want, or just use the hamburger later.

  return (
    <div>
      {/* Your full working Phoenix UI here */}
      {/* At the top you can add: */}
      <button onClick={onBack} className="text-orange-400 p-4">← Back to Dashboard</button>
      {/* Then the rest of your code */}
    </div>
  )
}

export default PhoenixLink