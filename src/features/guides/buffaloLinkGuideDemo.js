/**
 * Demo / fallback content for Buffalo Link until the row in `guides` is authored in Supabase.
 * Written for this app - not copied from third-party paywalled guides.
 */

export const BUFFALO_LINK_DEMO_SLUG = 'buffalo-link'

/** Short +EV-oriented bullets shown on the card (not the full playbook). */
export const buffaloLinkCardBullets = [
  'Hunt **strong buffalo-per-spin** and **healthy fusion / eagle meters** - weak signatures are usually a hard pass.',
  'Treat **midpoint / walk-away math** as mandatory: volatility is extreme; the in-app calculator encodes the regime logic.',
  '**Floor prevalence is very high** and **nerf risk is real** - verify paytables, denom, and progressive caps the same day you play.',
]

export const buffaloLinkGuideMarkdown = `## What you are looking at

Buffalo Link is an **Aristocrat persistent-state** title: what happened on the machine before you arrived **shows up on the meters and symbol density**. Your job is not to “get lucky on one spin” - it is to recognize **when the state implies a favorable RTP band** and to **size bets and stop wins** so variance does not erase the edge.

## The +EV picture (practical)

1. **Buffalo intensity** - Count how often buffalo heads land relative to your denom and bet. Strong games keep buffalo **visibly stacked** in the base reel strips over your session window; anemic screens rarely justify deep play.
2. **Fusion / eagle ladders** - These persistent ladders are where a lot of the **equity** hides. You want ladders that are **closer to completion** *only when* the incremental cost to finish still clears your hurdle rate (use the calculator rather than eyeballing).
3. **Progressive exposure** - Minors/majors and linked prizes **move with marketing**. Never assume yesterday’s cap is today’s; photograph paytables when material.

## How the game plays (operator-facing)

- **Base game:** Standard line pays with buffalo stacks; volatility means base can **feast or famine** for long stretches.
- **Features:** Bonus paths that build **counters, tokens, or meters** carry forward until hit or reset depending on jurisdiction and theme revision.
- **Denoms:** Always confirm **denom ↔ progressive eligibility** before locking a session plan.

## Bankroll and session hygiene

Buffalo Link can **draw down multiple majors** worth of volatility before converging. If you cannot survive the **95th percentile** bad streak at your chosen bet, you are not playing the edge - you are buying lottery tickets with extra steps.

## Using the Edge calculator

Open **Buffalo Link EV Calc** from the card. It is built around the same **midpoint / logistic-style exit** philosophy discussed in-product: use it to translate what you see on the glass into **concrete stop rules** instead of improvising under pressure.

---

*Replace or extend this text anytime by editing the \`guides.content_markdown\` row for Buffalo Link in Supabase.*
`
