# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — finish Stripe prod smoke on edgetilt.com.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge** / **EdgeTilt**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).

---

**Branch:** `test` ( **`origin/main`** and **`origin/test`** aligned at **`dea9eae`** as of **2026-07-01** )

**Supabase (do not swap these):**

| | Project ref | Domain |
| --- | --- | --- |
| **Production** | **`jtjgtucumuoswnbauxry`** | **`edgetilt.com`** |
| **Test sandbox** | **`kcosfvmreeiosdjdzycb`** | **`lvslotpro.com`** |

**Goal:** _Prod polish + remaining Stripe smoke; commit uncommitted legal fix; Starter weekly drop cron when ready._

**Start by reading:** `AGENTS.md`, then **`docs/test-buildout-backlog.md`** Update log (**2026-07-01** tail). For **Stripe prod:** **`docs/stripe-billing-test-to-prod-handoff.md`**. For **prod replay gaps:** **`docs/production-rollout-checklist.md`** §1–§6. Cutover runbook: **`docs/edgetilt-production-cutover.md`**.

**Optional add-ons** (delete lines you do not need)

- Session focus: _[e.g. Stripe upgrade smoke, commit legal modal fix, guide card bugs]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[e.g. "see backlog Update log 2026-07-01"]_

---

## Pick up here (Jul 2026)

**Latest commits (`main` / `test`):** `dea9eae` (Gmail-safe auth email header + template doc) · `9acf054` (Stripe portal opens billing home, not cancel deep-link) · `5af508f` (Stripe prod handoff doc) · `6680e25` (Settings legal link spacing) · `a5427cd` (light mode Settings/billing)

### Shipped on production (`edgetilt.com`) — recent

| Area | Status |
| --- | --- |
| **EdgeTilt cutover** | **`edgetilt.com`** → prod Supabase; **`lvslotpro.com`** → test sandbox (Ryan sign-off **2026-07-01**) |
| **Auth** | Google + email signup/reset; Resend SMTP **`noreply@auth.edgetilt.com`**; confirm + reset templates in Supabase (see **`docs/supabase-auth-email-templates.md`**) |
| **Stripe billing** | Live catalog, webhooks, 3 Edge fns; migrations **`20260701120000`–`20260701160000`**; minimal smoke **PASSED** (Starter monthly + founding coupon **`QnYlzKuK`**) |
| **Legal** | **`/terms`**, **`/privacy`**, **`/guidelines`** on **`edgetilt.com`** — counsel-reviewed; in-app routes = public legal URLs (Ryan **2026-07-01**); prod checklist §7 **closed** |
| **Subscribe / billing UX** | Plan picker, interval tabs, manage membership modal, light-mode contrast fixes |

### Next priorities (ordered)

1. **Stripe prod smoke matrix** — full interval/upgrade/Lifetime/portal cancel checklist in **`docs/stripe-billing-test-to-prod-handoff.md`** §Test plan
2. **Starter weekly guide drop** — schema + RPC shipped; **cron scheduler + reveal UX** still open (`20260701130000`)
3. **Billing RLS hardening** — entitlements (backlog)
4. **Bug hunt** — guide cards light/dark, mobile **Where to find** tables, whatever Ryan finds in smoke

**Do not commit:** untracked `public/edge-email-logo*` experiment PNGs/JPG unless Ryan asks.

### AP guides (ongoing workflow)

**Source of truth:** Supabase **`guides.content_markdown`** + R2 heroes. **Not** repo markdown.

**Editor:** **`/slot-guide-form`** on **`edgetilt.com`** (prod) or local **`npm run dev`**.

| Task | Steps |
| --- | --- |
| **New guide** | Admin login → sections → hero → **Ingest guide** |
| **Edit existing** | **Fetch guides** → **Load** → edit → **Save changes** |
| **WIP** | **Save draft** (localStorage only) |

**Critical:** Always **Fetch → Load** before editing. Empty **Where to find** on save wipes that section.

**Prod = prod for agents:** Never ingest/batch-run to **`jtjgtucumuoswnbauxry`** unless Ryan names the slug/batch. Backup first: **`node scripts/ap-guide-backup-test-guides.mjs --all-batch --target=production`**.

**Test DB:** **310** published guides; **`release_year`** + **`card_accent_color`** backfilled on test.

**Light mode trap:** zinc **`bg-zinc-950`** remaps pale in light mode; **`text-white`** on inputs does not — scope under **`html.light`** + **`data-*`** roots.

### Parked

- Thread compose iOS keyboard / options bar overlap after media pick
- Automated docx → guide ingest script
- Chat R2 video promote to prod; link-preview + group-delete smoke sign-off
- Multiple Stream clips per post (v2 cap: 2)
- Formal **`WAKEUP`** is not a substitute for backlog SQL apply order — grep migration dates in Update log before applying
