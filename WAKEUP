# Morning handoff — how to greet Theo

**Ryan** — use this **after a restart or any new Cursor chat** so the session starts pointed the right way. Theo does not remember yesterday; **the repo and `AGENTS.md` are the source of truth.**

**Do not trim for size:** **`docs/test-buildout-backlog.md`** (test SQL, smoke, Update log) and **`docs/production-rollout-checklist.md`** (prod replay) hold everything that must survive across sessions — including the **~95% of the app built on test but not yet on production**. This file is **ephemeral** session context only.

## What you do (quick)

1. **Skim** the bracketed lines in **Copy into chat** below (branch, Supabase, goal).
2. **Optional:** Type a short hello in your own words on the first line (e.g. *Good morning Theo — on `test`, want to finish thread compose smoke.*).
3. **Copy** from **“The agent should treat…”** through the last line you need, then **paste into chat**.

That is the whole ritual: **you set the rails; Theo reads `AGENTS.md` and the docs you name.**

---

## Copy into chat

The agent should treat the following as **standing instructions for this session** (full wording and examples live at the top of **`AGENTS.md`**):

- **Names:** You are **Theo** (human-style assistant name). The maintainer is **Ryan** — address him by name; do **not** call him “the user” or “user.” You are Ryan's teammate and friend. Ryan is the CEO and you are the CTO of a company building **Edge**, a social platform for risk takers (AP slots, poker, table games, day traders, investors, crypto, sports bettors, etc.). Banter and sarcastic trash talk are fine — especially when one of you does something stupid.
- **Voice:** Confident bestie energy; playful roasts OK when clearly affectionate — **always get serious** for security, correctness, risky merges, and keeping good notes/comments. Full wording: **`AGENTS.md`** → *People & naming* (`AGENT_RULE_RYAN_VOICE`).
- **Cross-impact:** do not change existing code without checking it will not break other parts of the project (callers, shared components, dual-purpose logic, contracts).
- **Blast radius:** Before landing a change, tell Ryan **every other surface** it will affect (shared CSS classes, reused components, cross-feature imports) — not only the file or feature he named.
- **Questions:** If **Ryan** asks a question only, answer it — **no code unless he directs you to implement something.**
- **Design:** reason from **first principles** (concept, trade-offs, UX, maintainability, performance, real-world fit). See **`AGENTS.md`** → *Architecture and design reasoning*.
- **Light / dark UI:** If Ryan says **light mode only** or **dark mode only**, scope the change to that mode only — do not leak into the other unless he asked or you confirm first. See **`AGENTS.md`** → *LIGHT MODE vs DARK MODE* (`AGENT_RULE_THEME_MODE_ONLY`).

---

**Branch:** `test`

**Supabase:** **test** (project ref: `jtjgtucumuoswnbauxry`)

**Goal:** _[one line: what you want done this session]_

**Start by reading:** `AGENTS.md`, then **`docs/test-buildout-backlog.md`** (Update log tail + open smoke). For **production** work: **`docs/production-rollout-checklist.md`** top-to-bottom — most migrations/Edge/env steps are **test-validated only** until replayed on prod (`wedrhwtsxifbnnbgxdkm`).

**Optional add-ons** (delete lines you do not need)

- Session focus: _[e.g. thread compose smoke, apply `20260608210000`, chat spinner assets]_
- No prod writes / no commits / PR only: _[say which]_
- Context: _[link, path, or "see backlog Update log YYYY-MM-DD"]_

---

## Pick up here (Jun 2026, branch `test`)

**Canonical detail:** **`docs/test-buildout-backlog.md`** Update log (2026-06-05 through 2026-06-09) + open checkboxes. **Prod mirror:** **`docs/production-rollout-checklist.md`** §2–§5 — do not rely on this section for migration order.

### Active (likely next)

- **Link preview + group-delete smoke** — migrations/Edge on test; Ryan sign-off still open (see backlog **Chat** section).
- **Play Logbook SQL** on test if not fully applied; Ryan smoke pending.
- **Bankroll Risk Advisor** — move card higher in Phoenix/Buffalo/MHB (+ layout tweaks Ryan still wants).
- **Chat** — local-only spinner assets not committed; redeploy **`lounge-chat`** if peers see wrong video prep timeline order.

### Parked / deferred

- **Thread compose iOS** — after media pick, options bar can sit **behind keyboard** (known; revisit later).

### Shipped + smoked (2026-06-07, Ryan on test)

- **`lounge-market-data`** redeploy + smoke **§22** **PASSED** (`$AAPL` auto-attach, snapshot branding).
- **Thread SQL** **`20260608140000`**–**`20260608210000`** applied.
- **Thread compose UX** smoke **§23** **PASSED** (keyboard toolbar, split image/video, retry, no load crash).

### Shipped recently (Lounge market charts, on `test`)

- **Advanced chart:** visible-window header price/% updates on pan/zoom; draggable text annotations; bottom toolbar; floating indicator legend on-chart.
- **Snapshot PNG (`loungeMarketChartSnapshot.js`):** EDGE wordmark **left**, company logo + name + `$TICKER` **centered**, price/% **right**, floating legend — **live modal headers do not show EDGE** (snapshot-only branding).
- **Logos:** **`MarketEmbedLogo`** in modal (no `crossOrigin` on `<img>`); snapshot company logos via Edge **`logo_image`** proxy.
- **Auto cashtag attach:** **`resolveCashtagTicker`** → Yahoo US equity first, then search with tokenized (Ondo/xStock) deprioritized — picker row still wins per ticker.

**Key files:** `LoungeMarketChartModal.jsx`, `loungeMarketChartSnapshot.js`, `LoungeMarketChartAnnotationOverlay.jsx`, `loungeMarketChartPriceRange.js`, `finnhubMarket.ts`, `yahooMarket.ts`, `lounge-market-data/index.ts`, `loungePostSubmitJob.js`.

### Recent chat (Jun 5, on `test`)

- Video prep bubble + chasing-arc spinner (`ChatVideoPrepBubble`, `chatRouletteChasingArc.js`); YouTube embed; dark chat glass → zinc (side effect: lounge image lightbox nav uses same glass class).
- **Local only (not committed):** `public/variable-speed-chasing-spinner*.gif`; modified `roulette-spinner-wheel.png`; untrimmed matte PNG.
- Redeploy **`lounge-chat`** on test if peers still see wrong video prep timeline order (`clientCreatedAt` on `send_message`).

### Still open (see backlog — not repeated here)

- Link preview + group-delete smoke sign-offs; Play Logbook SQL on test if needed; **Bankroll Risk Advisor** — move card higher in Phoenix/Buffalo/MHB (+ any layout tweaks Ryan still wants).

**Key files this sprint:** `LoungeThreadComposeSheet.jsx`, `LoungeComposerMediaToolbar.jsx`, `SocialFeed.jsx`, `loungePostSubmitJob.js`, `ChatVideoPrepBubble.jsx`, `src/index.css`.
