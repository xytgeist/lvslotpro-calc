# Frontend architecture

How the React app is organized after the **feature-module split** (2026). Use this when onboarding, finding code, or planning refactors.

## Entry and bootstrap

- **`src/main.jsx`** — mounts the root component (unchanged pattern). **`installGlobalTapHaptic()`** from **`src/utils/tapHaptic.js`** registers capture-phase **`click`** haptics only for **`data-tap-haptic`** opt-in nodes. Most feedback is wired explicitly (Lounge like/bookmark/repost/post/profile, title menu Slots/Chat, chat send, bankroll session start/end, guides Ask community, logbook, calculators, calendar event modal). Skips keyboard **`detail === 0`** activations and **`data-no-tap-haptic`** subtrees.
- **`src/App.jsx`** — **auth and session only**: Supabase client, login / signup / forgot password / reset password, OAuth callback handling, routing between auth panel vs the app shell, and **`browseMode`** (anonymous vs member). It imports **`AppShell`** when `currentView === 'app'`. **No client-side email whitelist** — any successful Supabase session is treated as a member for shell access (freemium / subscribe gates are separate).
- **`src/features/shell/AppShell.jsx`** — **logged-in application**: bottom navigation, tab state, Lounge feed wiring, Offers / Guides / Intel / Bankroll / Calculators / Team surfaces, global confirm modal, URL query handling for **Offers** deep links and **Lounge** deep links (`post` / `u` / legacy `profile` query → Home tab; share URLs use **`/lounge/p/<uuid>`** and **`/u/<handle>`** on Vercel for OG), and installed PWA notification prompt coordination (iOS + Android Home Screen / Install app).

Keeping auth in **`App.jsx`** and product chrome in **`AppShell`** avoids a circular dependency story and keeps the entry file small (~hundreds of lines instead of thousands).

## Brand palette (Tailwind + CSS)

Canonical **`src/index.css`**: **`lv-*`** Tailwind colors (**`lv-red`**, **`lv-orange`**, **`lv-yellow`**, **`lv-blue`**, **`lv-green`**, **`lv-purple`**) plus **`--lv-*`** CSS variables on **`:root`**. The **`cyan-*`** utility scale is overridden so existing **`cyan-*`** classes align with electric blue (**`#06cefc`**). Violet accents map toward vivid purple (**`#9d00ff`**). Dock FAB accents live in **`loungeDockFabGlow.js`**; carousel glyphs use **`LOUNGE_DOCK_CYAN`** in **`loungeDockArcCarouselItems.jsx`**.

## Access model (public browse + member)

- **Anonymous:** No Supabase session → user lands in **`AppShell`** immediately (Supabase **anon** key; public-read RLS paths work). A sticky strip invites **Log in**; Lounge and other surfaces follow **anon** policies (today: read-only patterns in Lounge where writes require auth).
- **Member:** Any authenticated Supabase session → **`browseMode === 'member'`** (log out, posting, offers sync, etc. as implemented). **`hasActiveSubscription`** = **`profiles.has_active_subscription`** OR env **`VITE_HAS_ACTIVE_SUBSCRIPTION`**; **`isStaff`** = **`profiles.role`** in `moderator` / `admin`. Those drive **hamburger lock icons** in `AppShell.jsx`. Testing: **`docs/test-user-roles.md`**, SQL **`supabase/profiles_tier_testing.sql`**.
- **Auth UI:** Log in / sign up / forgot password render as a **full-screen panel** when the user opens auth from the strip, **Continue without signing in**, or when a feature calls **`onRequireAuth`** (no full-app reload). Whitelist rejection sets a dismissible **`accessNotice`** in the shell after sign-out.

### Planned: freemium + subscription tiers

Roadmap detail: **`docs/social-feed-roadmap.md`** (section **Freemium & subscriptions**); **tier matrix:** **`docs/access-tiers.md`**. In short: **anonymous** stays read-heavy (e.g. Lounge view-only); **free accounts** unlock more non-premium actions; **subscribers** get subscribe-gated areas. When built, add a small **entitlements** layer (DB + optional RPC) and extend **`browseMode`** or pass **`entitlements`** into **`AppShell`** — **RLS/webhooks** remain the source of truth, not UI alone.

## Feature folders (`src/features/`)

| Folder | Role |
| --- | --- |
| **`shell/`** | `AppShell.jsx`, `shellClasses.js` (shared Tailwind class strings for layout/buttons where reused), `index.js` barrel. **Hamburger nav:** **Lounge**, **Slots** (hub), **Team** — slot tools (Calcs, Calendar, Bankroll, Logbook, AP Guides) open from **`slots/`** hub, not top-level menu rows. **`intel` tab** still lazy-loads but is **not** on the hub (Ryan, 2026-05-29). **Tab layout:** Dashboard / Team placeholders, **Bankroll**, **Local Intel**, and **Offers** use **`ScrollLinkedEdgeTitleBarShell`** (fixed EDGE logo + hamburger, scroll-linked hide/show) so they match Lounge / Guides / Calculators; the shell column is **`max-w-2xl`** + **`px-3`** (Offers week landscape can pass **`fullWidth`** for edge-to-edge). **iPhone:** shell uses **`bg-zinc-950`** and puts **`env(safe-area-inset-bottom)`** in scroll **content** padding (`calc(6rem+…)` default), not a non-scrolling gap under the scroller. |
| **`auth/`** | OAuth UI bits and URL callback helpers used by **`App.jsx`**. |
| **`legal/`** | **`/terms`**, **`/privacy`**, and **`/guidelines`** document pages (**`LegalDocumentScreen.jsx`**, **`legalDocuments.js`**); signup acceptance checkbox + **`profiles.terms_accepted_at`** / **`privacy_accepted_at`** / **`legal_policy_version`** (migration **`20260627200000`**). Policy version: **`legalPolicyVersion.js`**. |
| **`lounge/`** | Lounge social feed: **`SocialFeed.jsx`**, **`LoungePostCommentThread.jsx`** (post detail: comment rows mirror **`LoungePostArticle`** meta + **`LoungePostRowMenu`** ⋯ — authors **delete** replies only (comment edit UI disabled; DB **`feed_comments_update_own`** remains for future use); tap comment → stacked **Reply** screen with direct replies newest-first; tap-back returns), **`LoungePostArticle.jsx`**, **`LoungePostFeedMedia.jsx`**, **`LoungePostStreamVideo.jsx`** + **`LoungeStreamLightboxContext.jsx`** + **`loungeStreamLightboxRenderers.jsx`** (unified Stream + image/GIF full-screen chrome across feed/detail/comments/profile) + **`loungeLightboxImageZoom.js`** (pinch-to-zoom + pan on still/GIF lightbox) + **`LoungeFeedVideoAutoplayContext.jsx`** + **`loungeFeedVideoAutoplayStore.js`** (feed/embed: visibility-band autoplay, **`{prev, active, next}`** HLS ring max 3 decoders, feed-wide Tap for sound + 60%/40% audio bands, **hero lock** collapses ring; HLS via `hls.js` lazy chunk when needed), **`LoungeProfileFullScreen.jsx`**, **`LoungeOgBadge.jsx`**, **`LoungeFlameIcon.jsx`** + **`LoungeLikeStatContent`** (chip-heart like; fixed icon/count grid), **`loungeDockComposeFocus.js`**, **`loungeSearchApi.js`** (Phase G dock search RPCs), **`src/utils/loungeVideoUpload.js`**, **`src/utils/loungeCfImageMedia.js`** (R2 upload + delivery URLs; **`LOUNGE_CF_R2_OBJECT_CACHE_CONTROL`** on PUT), **`src/utils/communityFeedPost.js`** (`uploadLoungeFeedPostImage`, delete hosted media on R2 or legacy **`lounge-feed`**), **`src/utils/loungeSharePost.js`** (permalink + Web Share + clipboard), **`loungeCaption.jsx`** (**`renderRichCaption`**, hashtags/mentions) + **`splitTextWithLinks`** from **`linkifyText.jsx`** (bare domains + `https://` link colors); **`LoungeLinkPreviewBlock`** + post detail **`link_preview`** from **`loungeFeedScope`** select; attach on submit in **`loungePostSubmitJob.js`** / **`loungeCommentSubmitJob.js`**, caption helpers, **post threads** (**`LoungeThreadComposeSheet.jsx`**, **`LoungeComposerMediaToolbar.jsx`**, **`loungeThreadComposeMedia.js`**, **`loungeThreadComposeVideoPrep.js`**, **`loungeCommentLimits.js`**), **market chart embeds** (**`market_embeds`** jsonb — migrations **`20260609120000`**–**`20260609150000`**, max **12**): Edge **`lounge-market-data`** (`search`, `attach`, `batch_rolling`, `modal_series`, **`logo_image`**); feed **`LoungeMarketChartStrip`** + **`LoungeMarketFeedContext`**; modal **`LoungeMarketChartModal`** (Quick sheet + Advanced fullscreen, Lightweight Charts; Advanced **Add to post** confirm modal before composer insert); PNG snapshot **`loungeMarketChartSnapshot.js`** (**EDGE** wordmark left, company centered — **not** on live modal headers); cashtag dropdown **`loungeCashtagAutocomplete.js`**; attach after publish **`loungePostSubmitJob.js`**; auto **`$TICKER`** resolve **`resolveCashtagTicker`** (Yahoo equity first), storage keys, `index.js`. Rate-limit errors render **above** the composer. **Repost** uses an **anchored popover** above the control (not a bottom sheet). **Share** is in the post row **⋯** menu (`LoungePostRowMenu.jsx`), not the four-column interaction bar. Feed **media carousels** reset to the first slide when the post row re-enters the viewport. **Still images:** **`image_urls`** → **Cloudflare R2** on configured media subdomain (test: **`media-test.lvslotpro.com`**) via Edge **`lounge-cf-r2-direct-upload`**; delete via **`lounge-cf-r2-delete-object`** / orphan path. **Video:** up to **60s**, Cloudflare Stream; inline feed autoplay is **coordinated** (winner plays; staged neighbors buffer) with poster→first-frame **crossfade**; **no inline controls** on the frame — **single tap** opens **hero expand** full-screen (same `<video>`, GPU FLIP, swipe dismiss via **`loungeLightboxSwipeDismiss.js`**); **compact bottom-left** control: **small label pill** (same look as before) inside a **transparent** `min-h-[44px] min-w-[44px]` tap target so the rest of the tile still opens full screen (global when inside autoplay provider). Frame **`data-lounge-video-zoom`** excludes row-open on tap. **Feed load-more:** **`AppShell.jsx`** fetches **40** posts per page. **Opened post detail** is **`z-[98]`** (above **`LoungeProfileFullScreen`** at **`z-[97]`**); sticky bottom **reply composer** mirrors feed **collapse → expand**, **`focusLoungeComposerCaption`** / **`scheduleLoungeComposerTextareaFocus`**, scroll-to-top on focus; **`useLoungeKeyboardOverlapPx`** + **`visualViewport`** overlap **only while reply field is focused** (not on every post-detail open — avoids iOS footer floating mid-screen on long threads). **Quote repost** and **media lightboxes** sit **above** it (`z-[100]`+). Deleting the post invokes Edge **`lounge-cf-stream-delete-video`** before removing the row, then removes stored **`image_urls`** / **`stream_poster_url`** from **R2** or legacy **`lounge-feed`** via **`deleteLoungeFeedMediaFromPublicUrl`**. **Same-tab video post:** composer JPEG `blob:` poster is pinned in **`loungeStreamSessionPoster.js`** by Stream uid for immediate feed tile sizing, then swapped to CF `thumbnail.jpg` when ready. **Cross-device:** DB **`stream_poster_url`** + **`stream_video_width`** / **`stream_video_height`** (set in **`loungePostSubmitJob.js`** from session blob or capture + **`probeVideoFileDisplaySize`**) give every client a stable poster and aspect before CF thumb. **Chat (dock):** **`LoungeChatPanel.jsx`** in **`LoungeDockSlidePanels.jsx`**, **`loungeChatApi.js`** + Edge **`lounge-chat`**; profile **Message** in **`LoungeProfileFullScreen.jsx`**. **Own profile:** **Likes** / **Bookmarks** tabs (`post_likes` / `post_bookmarks`, same card layout as Posts). **Replies** tab: user's **`feed_comments`** + parent-post embed (`ProfileReplyRow`); tap → post detail + comment drill-down via **`SocialFeed.jsx`** `onOpenProfileReply`. **`LoungeOgBadge`** reads **`profiles.is_og`** (one-time backfill: **`supabase/profiles_is_og.sql`**). **Nav:** **`LoungeDockArcCarouselPrototype`** wired from **`SocialFeed.jsx`**; legacy **`LoungeDockFooterBar`** disabled (see `LOUNGE_DOCK_FOOTER_BAR_DISABLED`). |
| **`guides/`** | AP guides: `GuidesScreen.jsx` (uses **`ScrollLinkedEdgeTitleBarShell`** — same fixed EDGE title bar + scroll-hide as Lounge), demo data, `index.js`. **List perf:** card metadata loads first; full **`content_markdown`** fills in after paint (search skin index + expand cache); **24-card infinite scroll** via scroll-root sentinel (same pattern as Lounge feed). **Card accent:** **`guides.card_accent_color`** → **`guideCardAccent.js`** / **`guideAccentPalette.js`** (hex hero tint + legacy slug fallbacks). **Expanded card:** tap anywhere to collapse except links/buttons (**`guide-card-expanded-tap-collapse`**). **Ask Community:** modal posts to Lounge with guide attached; light mode scoped **`data-ask-community-*`** in **`index.css`**. Light mode: search **`ap-guides-search-input`**; expanded markdown h2 **`guide-section-heading`** + **`--gca-light-accent`**. Form preview: **`GuideCardPreview.jsx`**. |
| **`slot-guide-form/`** | **Admin AP Guide editor** — separate Vite entry, not in **`AppShell`**: **`SlotGuideFormApp.jsx`**, **`LoginGate.jsx`**, **`formUtils.js`**, **`guideFilePicker.jsx`**. Routes: dev **`/slot-guide-form`**, deploy same path. **Primary publish path (Jun 2026):** **`/slot-guide-form`** → Supabase **`guides.content_markdown`** + R2/Storage image URLs (see **`README.md`** § AP guide updates). **Ingest:** **`POST /api/slot-guide-ingest`**; **draft:** **`localStorage`** **`slotGuideFormDraft:v1`**; **edit:** **Fetch → Load → Save changes**. Optional **`where_to_find`** (**📍 Where to find**, after Risk). Card **Popularity** tile reads **`machines.popularity`** only. Guide heroes from DB **`thumbnail_url`** only (no **`public/guides/`** fallbacks). |
| **`offers/`** | Offers calendar: `OffersCalendar.jsx` (**`ScrollLinkedEdgeTitleBarShell`** for EDGE + scroll-hide; week landscape uses **`fullWidth`**), `offerStorageKeys.js`, `utils.js`, `hooks/`, `components/`, `index.js` (re-exports `OffersCalendar`). **Mailer photo import:** `hooks/useOffersCalendarMutations.js` → `handleImportPhotos` uploads to **Cloudflare R2** via `lounge-cf-r2-direct-upload` (same edge function as Lounge feed images); falls back to Supabase `offer-mailers` bucket when R2 is not configured. `offer_uploads.bucket_id = 'cf-r2'` signals R2; `storage_path` is the full R2 public URL in that case. Edge function `process-offer-uploads` fetches the image via HTTP when `bucket_id = 'cf-r2'`. `source_image_path` on `offer_events` stores the R2 public URL (or legacy Supabase path for older events); `WeekEventDetailModal` detects URL vs path and either displays directly or generates a Supabase signed URL. |
| **`intel/`** | Local Intel (cities / casinos / posts): `LocalIntel.jsx`, `index.js`. |
| **`bankroll/`** | **Bankroll Manager**: `BankrollTracker.jsx` (top-level, owns all state: sessions, adjustments, active tab), `BankrollSessionHistoryRow.jsx` (swipe-right-to-delete on session history; same gesture as chat inbox; disabled in Select mode), `index.js` barrel. **Tab nav:** OVERVIEW · LOCATIONS · CHARTS · TREND pill bar. **New tab files:** `BankrollTrendTab.jsx` (line chart, 7 time filters, adjustment markers), `BankrollChartsTab.jsx` (bar charts: weekday / month / year / session-length), `BankrollLocationsTab.jsx` (casino list sorted by P&L; taps open `LocationDetailModal` with cumulative P&L line, Type + Won/Lost donut charts, per-game-type P&L table). **DB columns:** `bankroll_sessions.game_type` (`slots`\|`tables`, default `'slots'`), `bankroll_adjustments` table (delta-logs manual bankroll edits for trend reconstruction). **Charting:** `chart.js` + `react-chartjs-2` (already in bundle, co-registered per tab file to avoid duplicate global state). `MoneyInput` + `GameTypeToggle` + `SheetHeader` defined locally at bottom of `BankrollTracker.jsx`. **BankrollTrendTab advanced features:** (1) **Advanced Metrics** — collapsible card (default collapsed, chevron toggle); 15 metrics including Max Drawdown, Ulcer Index, Sortino, Calmar, Profit Factor, Expectancy, Payoff Ratio, Win Rate, Std Dev, Skewness, Kelly, Recovery Factor, streak counts; each has an info modal except the obvious ones. (2) **Monte Carlo fan chart** — off by default; toggle button; `runMonteCarlo()` fn; 5 000 sims x selectable horizon (50/100/200); ruin threshold = 25% bankroll; p10-p90 + RoR; fan bands higher opacity in light mode (`isDark` via `matchMedia`). (3) **HTML tooltips** — canvas tooltip disabled; single tap on historical point shows DOM card (date, this-session P/L, cumulative P/L, "View details" opens `SessionDetailModal`); tap in fan area shows projected percentile callout (p10-p90 + RoR at that step); full-screen fixed backdrop dismisses on any outside tap; light/dark aware. |
| **`slots/`** | **Slots hub** (`SlotsScreen.jsx`): Calcs, Calendar, Bankroll, Logbook, AP Guides (subscriber locks on hub tiles). **Local Intel** not listed — **`intel/`** route kept in AppShell for possible future use. |
| **`chat/`** | **Chat** — full X.com-level DM + topic-channel + **group** surface. **`ChatTab.jsx`** (conversation list, lazy-loaded top-level tab; inbox actions via RPC **`chat_leave_room`**, **`chat_mark_room_unread`**, **`chat_set_room_pinned`**); **`ChatConversation.jsx`** (full-screen message view — Realtime INSERT/UPDATE subscriptions, paginated load-more, read receipts via `update_last_read`); **`ChatBubble.jsx`** (sent/received alignment, reply quote strip, image grid, reactions row, **`ChatLinkPreviewCard`** + **`YouTubeChatEmbed`** — URL-only = card-only bubble; text + link/YouTube = single bubble at full embed width); **`ChatVideoPrepBubble.jsx`** + **`chatRouletteChasingArc.js`** (optimistic video encode/upload placeholder — two-lap chasing arc spinner); **`ChatComposer.jsx`** (contentEditable + R2 image/video attach + typing broadcast + reply strip); **`ChatGroupSettingsSheet.jsx`** (group photo/title/members/mute; owner/admin **Delete Group for Everyone** via **`chat_delete_group`**; **Leave Group** via **`chat_leave_room`**); **`chatApi.js`** — Edge: `send_message`, `delete_message`, reactions, `open_dm`, group admin actions; RPC: **`chatLeaveRoom`**, **`chatDeleteGroup`**, pin/unread. **`chatTypingBroadcast.js`** (Supabase Realtime broadcast channel `chat-typing-{roomId}`, no DB writes, 3.5 s timeout). **Glass chrome:** **`index.css`** `.chat-header-glass` / `.chat-menu-glass` / sheets / gradients — dark = zinc-neutral; **also reused** by lounge image/GIF lightbox nav (**`LoungeStreamVideoLightboxChrome.jsx`**). **Link previews:** migrations **`20260604180000`**, **`20260604180100`**; Edge **`lounge-link-unfurl`**; shared **`src/components/ChatLinkPreviewCard.jsx`**, **`linkPreviewAccent.js`**, **`src/utils/youtubeEmbed.js`**. **Group lifecycle:** **`20260605120000_chat_group_delete.sql`** — empty-group trigger + **`chat_delete_group`**. **AppShell wiring:** lazy `ChatTab` chunk; `chat` in hamburger nav; `pendingChatPeerUserId` / `pendingChatRoomId` state; `?tab=chat&room=<uuid>` deep link; `onOpenChatWithUser` + `onOpenChatRoomFromDock` callbacks passed to SocialFeed. **Profile Message flow:** SocialFeed `openChatWithUserFromProfile` → AppShell → `open_dm`. **Dock panel:** `LoungeChatPanel.jsx` list-only; room tap → full Chat tab. **Quick link:** `chat` in `quickLinkDestinations.js`. **DB (baseline + groups):** Phase 2 **`20260601120000`**; group features **`20260603100000`**–**`20260603160000`** repair chain — see backlog. |
| **`play-logbook/`** | **Play Logbook** (`PlayLogbook.jsx`): AP play capture + analysis — game templates (Phoenix Link, Buffalo Link, **Buffalo Diamond**, Stack Up Pays, MHB + user custom + **admin primary game templates**), dynamic metric forms, LOG / ANALYZE tabs; **Phase 3:** calculator pre-fill (`playLogPrefill.js`, `CalculatorLogPlayButton`; MHB **`mhb_manufacturer`** / **`mhb_meter`** / **`must_hit_by`**; Buffalo Diamond **`green_fg`** / **`blue_fg`** / **`gold_fg`**), capture casino via `resolveDefaultCaptureCasino` (`nearbyCasinos.js` — active bankroll session casino else GPS nearest, same as Bankroll start), ANALYZE **Export CSV** (`playLogExport.js`). **Phase 4 — shared plays:** partners UI (`PlayLogPartnersSection.jsx`, `playLogPartners.js`, `playLogApi.js`); manager + **Paid**; RPC save/update/delete; Lounge Alerts `play_log_shared` → `/?tab=logbook&playLogEntry=`. **Labels:** Counter Start / Counter Pop / Cash In / Cash Out; optional spins/bonuses/EV ($) via **`playLogMetrics.js`** **`metricDefMap`**. **Calculator slugs:** Buffalo Link = **`buffalo-link`**; Buffalo Diamond = **`buffalo-diamond`** (migration **`20260610190000`**). DB migrations through **`20260610190000`** — see **`docs/test-buildout-backlog.md`** Play Logbook section. Slots hub tile **Logbook** (Slots Edge gated). Light mode: scoped `data-play-logbook*` rules in `index.css` (mirrors bankroll sheet pattern). |
| **`calculators/`** | **`CalculatorsTab.jsx`** (picker + lazy games); **`ScrollLinkedEdgeTitleBarShell`** for the same fixed EDGE bar + scroll-hide as Lounge/Guides. Game UIs in **`calculators/games/`** include Phoenix Link, Buffalo Link, Buffalo Diamond, Stack Up Pays, MHB, **WoF 4D Collector's Edition** (`wof-collectors-edition`). **`BankrollRiskAdvisor.jsx`** — shared bankroll risk section in Phoenix Link, Buffalo Link, MHB, and Stack Up Pays; reads `bankroll_profiles.overall_bankroll` via `supabaseClient` (threaded AppShell to CalculatorsTab to calculator); risk % stepper 1-5% in localStorage (`lvsp:bankrollRiskPct`, default 2%); shows bankroll/budget/play-cost tiles + coverage bar; when underfunded shows sell amount + Need Help button; Need Help modal pre-fills play details and sends a structured DM to the admin user (`role=admin`) via `lounge-chat` Edge (`open_dm` then `send_message`). Stack Up: `maxExpectedLoss` = max exposure bets × bet size when Current EV ≥ 99%; Need Help includes five meter counters + exposure target meter; slate `cardClassName`. Order: Current EV → Bankroll Risk → Log Play. **TODO:** move `BankrollRiskAdvisor` higher in Phoenix/Buffalo/MHB — those still render near bottom after disclaimer. |
| **`ops/`** | **Edge Monitor** (admin-only): **`EdgeMonitorScreen.jsx`** (in-app tab), **`EdgeMonitorDesktopPage.jsx`** (full-width **`/monitor`** route), shared **`EdgeMonitorDashboard.jsx`** + **`OpsMonitorCharts.jsx`**. Lazy tab **`monitor`** in **`AppShell.jsx`**; hamburger **Monitor** when **`isAdmin`**; deep links **`?tab=monitor`** (mobile tab) and **`/monitor`** (desktop page). Data: RPC **`admin_ops_monitor_snapshot()`** + **`admin_ops_monitor_live_pulse()`** (migrations **`20260703100000`**–**`20260703120000`**); external probes via Edge **`admin-ops-external-health`**. Alerts/runbooks: **`opsMonitorAlerts.js`**, **`opsMonitorRunbooks.js`**. Roadmap: **`docs/edge-monitor-roadmap.md`**. |
| **`bots/`** | **Bot management portal** (admin-only): **`BotManagementScreen.jsx`**, **`BotManagementPortal.jsx`**, **`BotProfileEditor.jsx`**, **`BotEditorialInbox.jsx`**, **`BotPostRepliesPanel.jsx`**, **`BotReplyOnPostPanel.jsx`**, **`BotComposeImagePicker.jsx`**, **`BotCreateWizard.jsx`**, **`botPortalApi.js`**, **`botPortalPostId.js`**, **`botComposeImages.js`**. Lazy tab **`bots`** in **`AppShell.jsx`**; **`?tab=bots`**. RPCs **`admin_lounge_bot_portal_snapshot`**, **`admin_lounge_bot_save_settings`**, **`admin_lounge_bot_publish_post`** (caption + up to 6 **`image_urls`**, migration **`20260707000000`**), **`admin_lounge_bot_post_comment`**. **Scott Share / odds:** calendar dropdown → **`invokeLoungeOddsIngest`**; **Run alert now** (per type / **`p_alert_kind`**) + poll actions → **`lounge-odds-poll`**. **X editorial:** **Transform a post** (URL + optional tweet text) → **`lounge-x-ingest`** (bearer optional for manual path). Manual **Post as** + **Reply on any post** (migration **`20260704220000`**). Spec: **`docs/lounge-bot-sports-odds.md`**, **`docs/lounge-bot-editorial-queue.md`**. |
| **`profiles/`** | Shared profile gate helpers for Lounge + Guides (`profileGate.js`, etc.). Profile saves and **`handle_changed_at`** / cooldown-aware selects for the Lounge profile editor. |

Shared non-feature code stays in **`src/utils/`**, **`src/components/`** (e.g. **`ChatLinkPreviewCard.jsx`**, `CalculatorDisclaimer`, **`LoungeDockArcCarouselPrototype.jsx`**, **`loungeDockArcCarouselItems.jsx`**, **`LoungeDockSlidePanels.jsx`**, **`LoungeDockFooterBar.jsx`** [disabled in feed], **`loungeDockFabGlow.js`**, **`loungeDockFabPosition.js`**, **`loungeDockChrome.js`**, **`loungeTitleRevealScroll.js`**), and **`src/constants/`** as before.

## Imports and barrels

Several features expose **`index.js`** so callers can import from `'./features/<name>'` instead of deep paths. Examples: `'./features/shell'`, `'./features/guides'`, `'./features/lounge'`, `'./features/offers'`.

## Performance note (bundling)

Splitting files **does not** automatically reduce the initial JavaScript download: Vite still follows **static** `import` edges from `App` → `AppShell` → feature screens.

**Implemented in-repo:**

- **`AppShell.jsx`** — Heavy tabs load with **`lazyRoute()`** + **`Suspense`** (`SocialFeed`, `OffersCalendar`, `GuidesScreen`, `BankrollTracker`, `PlayLogbook`, `LocalIntel`, `CalculatorsTab`, **`ChatTab`**, **`SlotsScreen`**, **`EdgeMonitorScreen`**). The initial chunk after login avoids parsing those modules until the user switches to that tab (each becomes its own async chunk).
- **Stale deploy / chunk MIME errors** — After a Vercel deploy, an open tab can request old hashed chunks; Vercel's SPA fallback returns **`index.html`** → **`'text/html' is not a valid JavaScript MIME type`**. App-wide mitigation: (1) **`lazyRoute()`** / **`importRoute()`** in **`lazyImportWithChunkReload.js`**; (2) **`installStaleChunkReloadListener()`** in **`main.jsx`** (chunk failure → one auto-reload); (3) **`installDeployVersionWatch()`** in **`appDeployVersion.js`** compares live **`index.html`** (`meta[name=edge-build-sha]`) on refocus + every ~5 min — **refocus:** **`AppUpdateAvailableBanner`** + silent reload ~20s later (cancel via **Not now**); **foreground interval:** immediate silent reload. **Convention:** never add raw **`lazy(() => import(...))`** without **`lazyRoute`**.
- **`CalculatorsTab.jsx`** — Each game screen is **`lazyRoute()`**-loaded so **Chart.js** and other game-only deps load only when that calculator is opened. Buffalo route/slug: **`buffalo-link`** (was **`buffalo`**).
- **`slotGuideForm` chunk** — Separate entry **`slot-guide-form.html`** → async bundle **`slotGuideForm-*.js`**; loads only when visiting **`/slot-guide-form`**, not on main app login path.
- **`SocialFeed.jsx`** — Lounge post rows use **`content-visibility: auto`** and **`containIntrinsicSize`** so the browser can skip much of the layout/paint work for off-screen cards (complements infinite scroll; not a full virtual list).

Further wins (if needed later): true list virtualization (`@tanstack/react-virtual` or similar) if the feed DOM grows very large.

## Docs that reference old paths

If you see **`src/calculators/`** in older notes or chat exports, that directory was removed; calculator components now live under **`src/features/calculators/games/`**. Lounge work that used to cite **`SocialFeed` inside `App.jsx`** now refers to **`src/features/lounge/SocialFeed.jsx`**.

## Maintaining documentation

Structural or bundling changes here should land in the **same PR** as the code. For **what else to update** (roadmap, backlog, prod checklist, chat vs repo as source of truth), follow the repo playbook **`AGENTS.md`** at the project root.

---

_Last aligned with the modular `App` + `AppShell` + `features/*` layout; Lounge row expanded 2026-05-14 for FAB wheel nav, chip-heart likes, and dock compose focus; 2026-05-15 adds Settings **`loungeDockMenuLayout:v1`** (wheel vs Edge L)._
