# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase CLI / slot sync — keep test & prod service keys out of git
.env.supabase.test
.env.supabase.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

supabase/.temp/
supabase/.branches/

# Per-machine offsets for transcript export (scripts/cursor-chat-export.mjs)
.cursor/chat-export-state-*.json

# Local-only chat handoff (scripts + Task Scheduler append here); not versioned — distill into docs per AGENTS.md
session-chat-export.md