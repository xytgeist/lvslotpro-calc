# AGS Must Hit By — advantage play notes (stub)

_Generated seed — edit freely. Sync to Supabase with `npm run slots:sync` when ready._

Folder slug is **`ags-must-hit-by`** (sync expects folder name ≡ `machine.slug`), matching demos and calculator routing.

## What you are hunting

**AGS mystery must-hit progressive** mechanics: increment rules, denomination effects, displayed seed vs inferred clock, and any title-specific quirks.

## +EV checklist (fill in)

- Photo **jackpot ladders** / meters and capture **eligible bet** wording.
- Note **increment behavior** per credit step you observe.

## Verify on the floor

- **`hero.webp`** is the local marquee hero (sync may later use `thumbnail_url` / CDN URLs).

## Nerf / marketing

Assume thresholds and branding can vary by jurisdiction — update this playbook when packaging changes.
